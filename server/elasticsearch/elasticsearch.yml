node:
  name: es
network:
  host: 0.0.0.0
discovery:
  type: single-node
cluster:
  name: ${CLUSTER_NAME}
bootstrap:
  memory_lock: true
ingest:
  geoip:
    downloader:
      enabled: false
xpack:
  security:
    enabled: true
